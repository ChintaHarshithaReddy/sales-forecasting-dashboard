import React from "react";
import ReactDOM from "react-dom/client";
import SalesForecastDashboard from "./SalesForecastDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SalesForecastDashboard />
  </React.StrictMode>
);
